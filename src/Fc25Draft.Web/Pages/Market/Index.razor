@page "/market"
@using Fc25Draft.Web.Models.Market
@using Fc25Draft.Web.Services
@using Fc25Draft.Web.Hubs
@inject MarketClient MarketClient
@inject MarketHubClient MarketHubClient

<PageTitle>Mercado</PageTitle>

<h3>Mercado</h3>

<MarketFilters />

@code {
    private Core.DTOs.MarketQueryVm query = new();
    private IReadOnlyList<Core.DTOs.MarketItemVm> items = Array.Empty<Core.DTOs.MarketItemVm>();
}
